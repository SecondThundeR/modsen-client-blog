import { type ReactNode } from "react";

import { YoutubeFrameWrapper } from "@/components";

export const modalData: Record<string, ReactNode> = {
  "about-us-video": <YoutubeFrameWrapper id="dQw4w9WgXcQ" />,
};
